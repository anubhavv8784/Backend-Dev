# Cookies and Sessions in Express

This branch contains separate Express route files for:

- Multi-step registration with sessions
- Language preference with cookies
- Secure admin panel with session auth and RBAC
- Session timeout warning
- Anonymous and authenticated cart migration

## Run

```bash
npm install
npm start
```
